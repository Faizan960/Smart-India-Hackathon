{
  "name": "aws-sentinel",
  "version": "1.0.0",
  "description": "AWS Sentinel monitoring dashboard",
  "scripts": {
    "start": "npx vercel dev --local --listen 3000",
    "check": "echo Frontend uses native ES modules",
    "test:frontend": "node --test tests/frontend/live_pipeline.test.mjs"
  },
  "dependencies": {
    "dotenv": "^18.0.0"
  }
}
