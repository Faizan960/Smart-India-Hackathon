numpy==2.3.5
pandas==2.3.3
scikit-learn==1.7.2
joblib==1.5.2

# AWS Sentinel real-data ML pipeline (ml/pipeline)
PyYAML==6.0.3        # data_config.yaml provider mapping (data_mapping.py)
pyarrow==25.0.1      # Parquet ingestion in data_loader.py
shap==0.52.0         # optional: exact SHAP attribution (falls back gracefully if absent)

# Test suite (tests/ml)
pytest==8.3.5
